<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Dashboard Overview</title>
</head>


<body class="p-6 pt-5">
    <%- include('../partials/goback.ejs')%>
    <header class="bg-white shadow p-4 mb-8 mt-6" >
        <h2 class="text-2xl font-semibold">Dashboard Overview</h2>
    </header>
   
    <p> <b> <%= user.fullName %> </b> Welcome to the Donor dashboard! Here you can manage appointments, post announcements, and more.</p>
    <table border="1" style="width: 100%; border-collapse: collapse;">
        <h2>Dashboard Overview</h2>
        <thead>
            <tr>
                <th>Description</th>
                <th>Data</th>
            </tr>
        </thead>
        <tbody>
            <% if (user) { %>
                <tr>
                    <td><strong>Name</strong></td>
                    <td><%= user.fullName %></td>
                </tr>
                <tr>
                    <td><strong>Email</strong></td>
                    <td><%= user.email %></td>
                </tr>
                <tr>
                    <td><strong>Role</strong></td>
                    <td><%= user.role %></td>
                </tr>
                <tr>
                    <td><strong>Province</strong></td>
                    <td><%= user.province %></td>
                </tr>
                <tr>
                    <td><strong>District</strong></td>
                    <td><%= user.district %></td>
                </tr>
                <tr>
                    <td><strong>Sector</strong></td>
                    <td><%= user.sector %></td>
                </tr>
                <!-- Add more rows for additional user data as necessary -->
            <% } else { %>
                <tr>
                    <td colspan="2">No user data found.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>